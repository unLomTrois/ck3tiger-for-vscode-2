// Re-export the main diagnostic generator function
export { generateDiagnostics } from './diagnosticGenerator';

// Export collection functions if needed outside
export { getDiagnosticCollection } from './collection'; 